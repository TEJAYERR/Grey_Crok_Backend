name: Keep Backend Alive

on:
  schedule:
    # Runs every 9 minutes
    - cron: '*/9 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Backend Server
        run: |
          curl -I https://grey-crok-backend.onrender.com/health

